pipeline {
    agent any
    stages {
        stage('Run') {
            steps {
                script {
                    echo "Выбранные сервисы: ${params.PRODUCT}, ${params.SERVICES}, ${params.ENVIRONMENT}  "
                        
                        }
                    }
                }
            }
        }